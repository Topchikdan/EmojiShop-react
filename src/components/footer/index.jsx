import styles from './footer.module.scss'

function Footer() {
    return (
        <section className={styles.footer}>
            <h2 className={styles.footer__title}>Copyright &copy; Smiles Shop</h2>
        </section>
    )
}

export default Footer;
